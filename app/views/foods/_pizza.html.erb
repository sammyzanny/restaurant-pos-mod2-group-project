<h4>Pizza</h4>
<ul>
<% @foods.where(food_type: "Pizza").each do |pizza|  %>
<li>
    <%= pizza.name %> - $<%= pizza.price %>
    <%= button_to 'Remove Item', pizza,  method: :delete, form: {style: 'display:inline-block;'} %><br><br>
</li>
<% end %>
</ul>